{
  "name": "secure-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker serving Access-authenticated identity info and country flags from a private R2 bucket.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
